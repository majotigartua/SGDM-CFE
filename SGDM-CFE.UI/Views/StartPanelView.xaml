<UserControl x:Class="SGDM_CFE.UI.Views.StartPanelView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SGDM_CFE.UI.Views"
             xmlns:r="clr-namespace:SGDM_CFE.UI.Resources"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Content="{x:Static r:Strings.StartPanelLabel}" FontWeight="Bold" Margin="10" Grid.Row="0" />
        <Grid Grid.Row="1" Margin="10">
        </Grid>
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Content="{x:Static r:Strings.EditUserButton}" Width="100" HorizontalAlignment="Right" Grid.Column="1" Click="EditUserButtonClick" />
        </Grid>
    </Grid>
</UserControl>